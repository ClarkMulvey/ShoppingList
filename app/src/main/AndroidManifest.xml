<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shoppinglist">

    <application
        android:name=".ShoppingListCustomApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingList">
        <activity android:name=".activities.ViewDefaultListsActivity"/>
        <activity
            android:name=".activities.EditDefaultListActivity"/>
        <activity
            android:name=".activities.CreateUpcomingTripListActivity"/>
        <activity
            android:name=".activities.EditUpcomingListActivity"
            android:parentActivityName=".activities.MainActivity"/>
        <activity
            android:name=".activities.ShoppingSessionActivity"/>
        <activity
            android:name=".activities.StartShoppingActivity"/>
        <activity
            android:name=".activities.ViewUpcomingListsActivity"/>
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>